{
  "name": "taxilibre",
  "version": "1.0.0",
  "description": "TaxiLibre - Plateforme VTC Global",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:passenger\" \"npm run dev:driver\" \"npm run dev:admin\"",
    "dev:backend": "cd backend && npm run dev",
    "debug:backend": "cd backend && nodemon --inspect src/server.js",
    "dev:passenger": "cd apps/passenger-web && npm install && npm run dev",
    "dev:driver": "cd apps/driver-web && npm install && npm run dev",
    "dev:admin": "cd apps/admin-dashboard && npm install && npm run dev",
    "build": "npm run build:backend && npm run build:apps",
    "build:backend": "cd backend && npm run build",
    "build:apps": "npm run build:passenger && npm run build:driver && npm run build:admin",
    "build:passenger": "cd apps/passenger-web && npm install && npm run build",
    "build:driver": "cd apps/driver-web && npm install && npm run build",
    "build:admin": "cd apps/admin-dashboard && npm install && npm run build",
    "start": "cd backend && npm start",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "migrate": "cd backend && npm run migrate",
    "seed": "cd backend && npm run seed"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "concurrently": "^8.2.2",
    "dotenv": "^17.4.2",
    "jest": "^30.4.2",
    "prisma": "^6.19.3",
    "ts-jest": "^29.4.11"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0",
    "@supabase/supabase-js": "^2.105.4",
    "firebase-admin": "^10.3.0",
    "stripe": "^22.3.0",
    "uuid": "^14.0.1"
  }
}
